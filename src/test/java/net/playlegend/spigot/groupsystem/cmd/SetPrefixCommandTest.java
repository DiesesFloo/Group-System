package net.playlegend.spigot.groupsystem.cmd;

import net.playlegend.spigot.groupsystem.commands.cmd.SetPrefixCommand;

public class SetPrefixCommandTest extends CommandTest {
    protected SetPrefixCommandTest() {
        super(new SetPrefixCommand());
    }
}
